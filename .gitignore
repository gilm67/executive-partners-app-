# Node
node_modules/
.next/
out/

# Env & OS
.env*
.DS_Store

# Python
.venv/
__pycache__/
*.pyc

# Streamlit local secrets (never commit!)
// local-only secrets live here; production secrets go to Vercel env vars
.streamlit/

# Secrets / tokens (never commit!)
.tokens/
.vercel/
service_account.json

# Local Streamlit secrets (only for local dev)
.streamlit/

# local secrets
private_key.b64
.env.local
.vercel
.env*.local
!public/*.json
!public/*.json
.github/workflows/fly-deploy.yml
