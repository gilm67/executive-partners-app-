{/* inside app/markets/page.tsx cards */}
<ul className="space-y-1 text-sm text-neutral-300">
  {(m.ecosystem ?? []).slice(0, 2).map((x, i) => (
    <li key={i} className="flex gap-2">
      <span className="mt-1 h-1.5 w-1.5 rounded-full bg-emerald-400" />
      <span>{x}</span>
    </li>
  ))}
</ul>
