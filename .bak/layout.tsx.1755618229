// app/layout.tsx
import "./globals.css";
import Link from "next/link";

export const metadata = {
  title: "Executive Partners",
  description:
    "International & Swiss Private Banking — HNW/UHNWI | Executive Partners",
};

/** Top navigation — single instance only */
function TopNav() {
  return (
    <header className="sticky top-0 z-40 bg-black/90 backdrop-blur supports-[backdrop-filter]:bg-black/75">
      {/* main row */}
      <div className="mx-auto flex h-14 max-w-7xl items-center justify-between px-4">
        {/* Brand + strapline */}
        <div className="flex items-baseline gap-2">
          <Link
            href="/"
            className="text-lg font-extrabold tracking-tight text-white"
          >
            Executive Partners
          </Link>
          <span className="text-sm italic text-neutral-400">
            International &amp; Swiss Private Banking — HNW/UHNWI
          </span>
        </div>

        {/* Menu */}
        <nav className="flex items-center gap-6 text-sm">
          <Link href="/candidates" className="font-semibold text-white hover:text-neutral-300">
            Candidates
          </Link>
          <Link href="/hiring-managers" className="font-semibold text-white hover:text-neutral-300">
            Hiring Managers
          </Link>
          <Link href="/bp-simulator" className="font-semibold text-white hover:text-neutral-300">
            BP Simulator
          </Link>
          <Link href="/jobs" className="font-semibold text-white hover:text-neutral-300">
            Jobs
          </Link>
          <Link href="/top-talent" className="font-semibold text-white hover:text-neutral-300">
            Top Talent
          </Link>
        </nav>
      </div>

      {/* subtle divider under the top bar */}
      <div className="h-px w-full bg-white/10" />
    </header>
  );
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="bg-black text-neutral-200">
      <body className="min-h-dvh">
        <TopNav />
        {/* Ensure content is never hidden behind the sticky header */}
        <main className="relative z-0">{children}</main>
      </body>
    </html>
  );
}
