'use client';

export default function Header() {
  return (
    <header className="sticky top-0 z-50 border-b border-white/10 bg-[#0B0E13]/95 backdrop-blur">
      {/* Local nav removed — using global TopNav from app/layout.tsx */}
      {/* You can add hero text, banner, or leave empty */}
    </header>
  );
}